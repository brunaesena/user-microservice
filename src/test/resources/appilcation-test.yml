spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/user_service_test
    username: test_user
    password: test_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  test:
    database:
      replace: none
