spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/user_service_test
    username: test_user
    password: test_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  test:
    database:
      replace: none